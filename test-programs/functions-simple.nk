変数 add は (x, y) => x + y
一致を確認(3)(add(1, 2))

変数 subtract は (x, y) => x - y
一致を確認(-1)(subtract(1, 2))

変数 multiply は (x, y) => x * y
一致を確認(2)(multiply(1, 2))

変数 division は (x, y) => x / y
一致を確認(1)(division(2, 2))

/* remainder or modulus operator is not supported?
変数 reminder(x, y) は x % y
一致を確認(1)(reminder(7, 3))
*/
