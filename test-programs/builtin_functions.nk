// 文字列関数
変数 pattern は "[0-9]+"
一致を確認("F")(substring("FOO", 0, 1))
一致を確認("R")(at("BAR", 2))
一致を確認(true)(matches("199", pattern))
一致を確認(true)(matches("200", pattern))
一致を確認(false)(matches("a", pattern))
変数 sub は substring
一致を確認("B")(sub("BAR", 0, 1))

// 数値関数
一致を確認(1.4142135623730951)(sqrt(2.0))
一致を確認(3.0)(sqrt(9.0))
一致を確認(3)(int(3.14159265359))
一致を確認(10.0)(double(10))
一致を確認(1)(floor(1.5))
一致を確認(-1)(floor(-1.5))
一致を確認(5)(ceil(4.4))
一致を確認(5)(ceil(4.5))
一致を確認(-4)(ceil(-4.4))
一致を確認(-4)(ceil(-4.5))
一致を確認(10.5)(abs(10.5))
一致を確認(10.5)(abs(-10.5))

// 確認関数
assert(2 == 1 + 1)
// assert(3 > 5) // => throws AssertionError

変数 add は (x, y) => {
  x + y
}
変数 time は stopwatch( => {
  sleep(1000)
  表示("1")
})
表示("it took #{time} milli seconds")
エラー表示("this param is displayed into standard error")
一致を確認(5)(add(2, 3))
変数 list は new java.util.ArrayList
list->add(4)
list->add(1)
list->add(2)
list->add(3)
一致を確認(リスト(4 1 2 3))((list :> リスト<整数>))
一致を確認(5.0)(sqrt(3.0 * 3.0 + 4.0 * 4.0))
一致を確認(6)(int(6.5))
一致を確認(7)(floor(7.5))
